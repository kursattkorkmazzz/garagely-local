import { AppText } from "@/components/ui/app-text";
import { AppView } from "@/components/ui/app-view";

export default function GaragePage() {
  return (
    <AppView>
      <AppText>Garage Page</AppText>
    </AppView>
  );
}
